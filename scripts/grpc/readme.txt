Pruebas gRPC para no escribir los comandos en la terminal:

Hay que acceder a "cd scripts\grpc" para correr los comandos

Login
   .\login.ps1 -Username TU_USUARIO -Password "TU_CLAVE"

Listar formularios por gRPC
   .\listar-formularios.ps1

Crear formulario
   .\crear-formulario.ps1